machine:
  services:
    - docker

dependencies:
  pre:
    - curl https://install.meteor.com/ | sh
  override:
    - npm i --production
    - meteor build ../target --directory --server-only --architecture os.linux.x86_64
    - cp Dockerfile.prod ../target/Dockerfile

test:
  override:
    - echo "no tests!"

deployment:
  hub:
    branch: single-tenant
    commands:
      - docker build -t fleetr/web:single-tenant ../target
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
      - docker push fleetr/web:single-tenant
